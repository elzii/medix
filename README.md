# Electron Notes

No SCSS, so just use SASS and these NPM scripts:

```json
{
  "scripts": {
    "sass:build": "sass src/styles/main.scss src/views/main.css --no-source-map",
    "sass:watch": "sass --watch src/styles/main.scss src/views/main.css",
  }
}
```
